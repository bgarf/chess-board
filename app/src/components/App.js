const App = () => {
    return <Board />
}

// const root = document.getElementById('root')
// const sqNumbers = []
// for(let i = 1; i <= 64; i++) {sqNumbers.push(i)}
// const squares = squares.map((value, index) => {
//             <div key="{index}" className="{value}"/>
//         })
// console.log(squares)
// const board = (
//     <div id="board">
//         {squares.map((value, index) => {
//             <div key="{index}" className="{value}"/>
//         })}
//     </div>
// )